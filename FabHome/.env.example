# Copy this file to .env before first deployment.
# Keeps deployment settings stable across updates.

FABHOME_PORT=3001
FLASK_DEBUG=0
# Set a stable secret in production to preserve sessions across restarts.
FABHOME_SECRET=change-me-with-a-long-random-value
